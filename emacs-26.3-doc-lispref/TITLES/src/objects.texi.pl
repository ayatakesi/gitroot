#!/usr/bin/perl
# This script requires libintl-perl(>=0.90).
use Locale::TextDomain ("objects.texi" => "TITLES/");
my ($en, $ja);
while (<>) {
	($en, $ja) = (quotemeta('@chapter Lisp Data Types'), __ '@chapter Lisp Data Types'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Printed Representation and Read Syntax'), __ '@section Printed Representation and Read Syntax'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Comments'), __ '@section Comments'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Programming Types'), __ '@section Programming Types'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Integer Type'), __ '@subsection Integer Type'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Floating-Point Type'), __ '@subsection Floating-Point Type'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Character Type'), __ '@subsection Character Type'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsubsection Basic Char Syntax'), __ '@subsubsection Basic Char Syntax'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsubsection General Escape Syntax'), __ '@subsubsection General Escape Syntax'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsubsection Control-Character Syntax'), __ '@subsubsection Control-Character Syntax'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsubsection Meta-Character Syntax'), __ '@subsubsection Meta-Character Syntax'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsubsection Other Character Modifier Bits'), __ '@subsubsection Other Character Modifier Bits'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Symbol Type'), __ '@subsection Symbol Type'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Sequence Types'), __ '@subsection Sequence Types'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Cons Cell and List Types'), __ '@subsection Cons Cell and List Types'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsubsection Drawing Lists as Box Diagrams'), __ '@subsubsection Drawing Lists as Box Diagrams'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsubsection Dotted Pair Notation'), __ '@subsubsection Dotted Pair Notation'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsubsection Association List Type'), __ '@subsubsection Association List Type'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Array Type'), __ '@subsection Array Type'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection String Type'), __ '@subsection String Type'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsubsection Syntax for Strings'), __ '@subsubsection Syntax for Strings'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsubsection Non-@acronym{ASCII} Characters in Strings'), __ '@subsubsection Non-@acronym{ASCII} Characters in Strings'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsubsection Nonprinting Characters in Strings'), __ '@subsubsection Nonprinting Characters in Strings'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsubsection Text Properties in Strings'), __ '@subsubsection Text Properties in Strings'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Vector Type'), __ '@subsection Vector Type'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Char-Table Type'), __ '@subsection Char-Table Type'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Bool-Vector Type'), __ '@subsection Bool-Vector Type'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Hash Table Type'), __ '@subsection Hash Table Type'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Function Type'), __ '@subsection Function Type'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Macro Type'), __ '@subsection Macro Type'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Primitive Function Type'), __ '@subsection Primitive Function Type'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Byte-Code Function Type'), __ '@subsection Byte-Code Function Type'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Record Type'), __ '@subsection Record Type'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Type Descriptors'), __ '@subsection Type Descriptors'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Autoload Type'), __ '@subsection Autoload Type'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Finalizer Type'), __ '@subsection Finalizer Type'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Editing Types'), __ '@section Editing Types'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Buffer Type'), __ '@subsection Buffer Type'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Marker Type'), __ '@subsection Marker Type'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Window Type'), __ '@subsection Window Type'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Frame Type'), __ '@subsection Frame Type'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Terminal Type'), __ '@subsection Terminal Type'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Window Configuration Type'), __ '@subsection Window Configuration Type'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Frame Configuration Type'), __ '@subsection Frame Configuration Type'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Process Type'), __ '@subsection Process Type'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Thread Type'), __ '@subsection Thread Type'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Mutex Type'), __ '@subsection Mutex Type'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Condition Variable Type'), __ '@subsection Condition Variable Type'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Stream Type'), __ '@subsection Stream Type'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Keymap Type'), __ '@subsection Keymap Type'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Overlay Type'), __ '@subsection Overlay Type'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Font Type'), __ '@subsection Font Type'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Read Syntax for Circular Objects'), __ '@section Read Syntax for Circular Objects'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Type Predicates'), __ '@section Type Predicates'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Equality Predicates'), __ '@section Equality Predicates'); s/$en/$ja/;
	print;
}
