#!/usr/bin/perl
# This script requires libintl-perl(>=0.90).
use Locale::TextDomain ("text.texi" => "TITLES/");
my ($en, $ja);
while (<>) {
	($en, $ja) = (quotemeta('@chapter Text'), __ '@chapter Text'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Examining Text Near Point'), __ '@section Examining Text Near Point'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Examining Buffer Contents'), __ '@section Examining Buffer Contents'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Comparing Text'), __ '@section Comparing Text'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Inserting Text'), __ '@section Inserting Text'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section User-Level Insertion Commands'), __ '@section User-Level Insertion Commands'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Deleting Text'), __ '@section Deleting Text'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section User-Level Deletion Commands'), __ '@section User-Level Deletion Commands'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section The Kill Ring'), __ '@section The Kill Ring'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Kill Ring Concepts'), __ '@subsection Kill Ring Concepts'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Functions for Killing'), __ '@subsection Functions for Killing'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Yanking'), __ '@subsection Yanking'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Functions for Yanking'), __ '@subsection Functions for Yanking'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Low-Level Kill Ring'), __ '@subsection Low-Level Kill Ring'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Internals of the Kill Ring'), __ '@subsection Internals of the Kill Ring'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Undo'), __ '@section Undo'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Maintaining Undo Lists'), __ '@section Maintaining Undo Lists'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Filling'), __ '@section Filling'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Margins for Filling'), __ '@section Margins for Filling'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Adaptive Fill Mode'), __ '@section Adaptive Fill Mode'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Auto Filling'), __ '@section Auto Filling'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Sorting Text'), __ '@section Sorting Text'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Counting Columns'), __ '@section Counting Columns'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Indentation'), __ '@section Indentation'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Indentation Primitives'), __ '@subsection Indentation Primitives'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Indentation Controlled by Major Mode'), __ '@subsection Indentation Controlled by Major Mode'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Indenting an Entire Region'), __ '@subsection Indenting an Entire Region'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Indentation Relative to Previous Lines'), __ '@subsection Indentation Relative to Previous Lines'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Adjustable Tab Stops'), __ '@subsection Adjustable Tab Stops'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Indentation-Based Motion Commands'), __ '@subsection Indentation-Based Motion Commands'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Case Changes'), __ '@section Case Changes'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Text Properties'), __ '@section Text Properties'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Examining Text Properties'), __ '@subsection Examining Text Properties'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Changing Text Properties'), __ '@subsection Changing Text Properties'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Text Property Search Functions'), __ '@subsection Text Property Search Functions'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Properties with Special Meanings'), __ '@subsection Properties with Special Meanings'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Formatted Text Properties'), __ '@subsection Formatted Text Properties'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Stickiness of Text Properties'), __ '@subsection Stickiness of Text Properties'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Lazy Computation of Text Properties'), __ '@subsection Lazy Computation of Text Properties'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Defining Clickable Text'), __ '@subsection Defining Clickable Text'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Defining and Using Fields'), __ '@subsection Defining and Using Fields'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Why Text Properties are not Intervals'), __ '@subsection Why Text Properties are not Intervals'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Substituting for a Character Code'), __ '@section Substituting for a Character Code'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Registers'), __ '@section Registers'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Transposition of Text'), __ '@section Transposition of Text'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Replacing Buffer Text'), __ '@section Replacing Buffer Text'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Dealing With Compressed Data'), __ '@section Dealing With Compressed Data'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Base 64 Encoding'), __ '@section Base 64 Encoding'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Checksum/Hash'), __ '@section Checksum/Hash'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section GnuTLS Cryptography'), __ '@section GnuTLS Cryptography'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Format of GnuTLS Cryptography Inputs'), __ '@subsection Format of GnuTLS Cryptography Inputs'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection GnuTLS Cryptographic Functions'), __ '@subsection GnuTLS Cryptographic Functions'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Parsing HTML and XML'), __ '@section Parsing HTML and XML'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Document Object Model'), __ '@subsection Document Object Model'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Atomic Change Groups'), __ '@section Atomic Change Groups'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Change Hooks'), __ '@section Change Hooks'); s/$en/$ja/;
	print;
}
